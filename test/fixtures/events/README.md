This directory contains events for testing with [aws-lambda-local](https://www.npmjs.com/package/aws-lambda-local):

```
$ PAT="<GitHub Personal Access Token>" lambda-local -f index -e test/fixtures/events/<event>.json
```
