This directory contains events for testing with [lambda-local](http://ashiina.github.io/2015/01/lambda-local/):

```
$ PAT="<GitHub Personal Access Token>" lambda-local -f index -e events/<event>.json
```
